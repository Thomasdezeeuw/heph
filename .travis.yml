cache: cargo
os:
  - linux
  - osx
language: rust
env:
  - RUST_BACKTRACE=full
rust:
  - nightly
matrix:
  fast_finish: false
script:
  - cargo --version
  - rustc --version
  - cargo test --verbose --all-features
